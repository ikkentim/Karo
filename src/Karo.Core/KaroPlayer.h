#pragma once

#include "BoardPlayer.h"
namespace Karo {
    namespace Core {
        public enum class KaroPlayer {
            None = PLAYER_NONE,
            Player1 = PLAYER_PLAYER1,
            Player2 = PLAYER_PLAYER2
        };
    }
}